---
import { routePath } from '../utils/paths';
const pathname = Astro.url.pathname;
const links = [
  { href: routePath('/'), label: 'Home' },
  { href: routePath('/essays'), label: 'Essays' },
  { href: routePath('/categories'), label: 'Categories' },
  { href: routePath('/about'), label: 'About' }
];

const home = routePath('/');
const isActive = (href: string) => (href === home ? pathname === href : pathname.startsWith(href));
---

<header class="site-header">
  <div class="accent-line"></div>
  <div class="site-container">
    <nav>
      <a class="brand-wrap" href={routePath("/")}>
        <span class="brand">Katsu Essays</span>
        <span class="tag">Personal Essay Archive</span>
      </a>
      <div class="menu">
        {links.map((link) => (
          <a href={link.href} class:list={["menu-link", { active: isActive(link.href) }]}>{link.label}</a>
        ))}
      </div>
    </nav>
  </div>
</header>
